# Vercel Deployment Environment Variables
# Only these variables are needed for server deployment
# All API keys and OAuth credentials are stored client-side

# Session secret for secure sessions (REQUIRED)
SESSION_SECRET=your_random_session_secret_here

# Optional: Port (Vercel handles this automatically)
PORT=3000

# =================================================================
# LOCAL DEVELOPMENT ONLY (Not needed for Vercel deployment)
# =================================================================
# For local development, you can still use server-side env vars:
# GOOGLE_CLIENT_ID=your_client_id
# GOOGLE_CLIENT_SECRET=your_client_secret
# REDIRECT_URI=http://localhost:3000/auth/google/callback
# CHIPP_API_KEY=your_api_key
# CLICKUP_CLIENT_ID=your_client_id
# CLICKUP_CLIENT_SECRET=your_client_secret
# CLICKUP_REDIRECT_URI=http://localhost:3000/auth/clickup/callback

# =================================================================
# VERCEL DEPLOYMENT
# =================================================================
# For Vercel deployment, users configure these in the Settings page:
# - Google OAuth credentials
# - Chipp API key
# - ClickUp OAuth credentials
# All stored securely in browser localStorage
